## Readme

Note that the following code is just a bunch of errors, used as practice for an upcoming test.
This code is meant to be wrong, not compilable and/or executable.
Its purpose is just practice around error codes.
The errors focused are the one with comments, the rest like "duplicate class found" are totally ignored

### Test 1-3
These files are the first type of exercise, where you have to find the error type and where it is
### Test 4-8
In these files you have to determine the output of the code.  
<ins>Note:</ins> compilation and execution of these files in this folder might not work due to access modifiers 
